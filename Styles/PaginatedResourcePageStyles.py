"""
paginated_resource_page-specific styles
Contains styles that are unique to paginated_resource_page and defined inline in the file.
"""

# PaginationControls styles (extracted from inline styles in paginated_resource_page.py)

PAGINATION_CONTROLS_FRAME_STYLE = """
            QFrame {
                background-color: #f8f9fa;
                border: 1px solid #dee2e6;
                border-radius: 4px;
                padding: 8px;
            }
        """
